"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const application = require("tns-core-modules/application");
let com;
class LocateAddress {
    isPackageInstalled() {
        try {
            let intent = new android.content.Intent(android.content.Intent.ACTION_VIEW, android.net.Uri.parse("http://maps.google.com/maps"));
            let pm = com.tns.NativeScriptApplication.getInstance().getPackageManager();
            return intent.resolveActivity(pm) != null;
        }
        catch (e) {
        }
        return true;
    }
    available() {
        return new Promise((resolve, reject) => {
            resolve(this.isPackageInstalled());
        });
    }
    ;
    locate(options) {
        return new Promise((resolve, reject) => {
            if (!this.isPackageInstalled()) {
                reject("Maps app not installed, use 'available' before using 'navigate'.");
                return;
            }
            let addr = "";
            if (options.address) {
                addr = options.address;
            }
            else if (options.lat && options.lng) {
                addr = options.lat + "," + options.lng;
            }
            let url = "geo:0,0?q=" + (options.address ? encodeURIComponent(addr) : addr);
            let intent = new android.content.Intent(android.content.Intent.ACTION_VIEW, android.net.Uri.parse(url));
            (application.android.foregroundActivity || application.android.startActivity).startActivityForResult(intent, 0);
            resolve();
        });
    }
}
exports.LocateAddress = LocateAddress;
